package com.sunbase.customercrud;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class for the Customer CRUD application.
 * This class contains the main method that starts the Spring Boot application.
 */
@SpringBootApplication
public class CustomercrudApplication {

	/**
	 * Main method to start the application.
	 *
	 * @param args command-line arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(CustomercrudApplication.class, args);
	}
}
